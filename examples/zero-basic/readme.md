### zero-basic example

This example demonstrates the first step to spin up the basic web app using the `zero` framework. As we are going to connect to database and retrieve in this example.

Prefer to read these steps to get started with [basic example](https://im-ng.github.io/zero-docs/hello-zero.html).
